
  apiVersion: rbac.authorization.k8s.io/v1
  kind: ClusterRole
  metadata:
    name: local-disk-manager
  rules:
    - apiGroups: [ "*" ]
      resources:
        - "*"
      verbs: [ "*" ]
    - nonResourceURLs: [ "*" ]
      verbs: [ "*" ]
